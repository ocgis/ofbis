#ifndef _COMMON_H_
#define _COMMON_H_

#define	MIN(a,b)	( (a) < (b) ? (a) : (b) )
#define MAX(a,b)	( (a) > (b) ? (a) : (b) )

#ifndef NULL
#define NULL (void *)0L
#endif /* NULL */

#endif /* _COMMON_H_ */
